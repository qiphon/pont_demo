{
  "name": "demo1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "reGen": "rm -rf ./src/services && pont init",
    "build": "vite build",
    "rebuild": "npm run reGen && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "pont-engine": "0.1.45"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "vite": "^5.2.11"
  }
}
